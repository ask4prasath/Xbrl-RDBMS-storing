<%= form_for(@niat) do |f| %>
  <% if @niat.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@niat.errors.count, "error") %> prohibited this niat from being saved:</h2>
    <ul>
    <% @niat.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :company_id %><br />
    <%= f.text_field :company_id %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
