<h1>Listing operating_expenses</h1>

<table>
  <tr>
    <th>Company</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @operating_expenses.each do |operating_expense| %>
  <tr>
    <td><%= operating_expense.company_id %></td>
    <td><%= operating_expense.value %></td>
    <td><%= link_to 'Show', operating_expense %></td>
    <td><%= link_to 'Edit', edit_operating_expense_path(operating_expense) %></td>
    <td><%= link_to 'Destroy', operating_expense, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Operating expense', new_operating_expense_path %>
