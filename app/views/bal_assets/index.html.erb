<h1>Listing bal_assets</h1>

<table>
  <tr>
    <th>Company</th>
    <th>Value</th>
    <th>Start date</th>
    <th>End date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bal_assets.each do |bal_asset| %>
  <tr>
    <td><%= bal_asset.company_id %></td>
    <td><%= bal_asset.value %></td>
    <td><%= bal_asset.start_date %></td>
    <td><%= bal_asset.end_date %></td>
    <td><%= link_to 'Show', bal_asset %></td>
    <td><%= link_to 'Edit', edit_bal_asset_path(bal_asset) %></td>
    <td><%= link_to 'Destroy', bal_asset, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bal asset', new_bal_asset_path %>
