<%= form_for(@sample3) do |f| %>
  <% if @sample3.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@sample3.errors.count, "error") %> prohibited this sample3 from being saved:</h2>
    <ul>
    <% @sample3.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
