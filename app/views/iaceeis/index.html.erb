<h1>Listing iaceeis</h1>

<table>
  <tr>
    <th>Company</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @iaceeis.each do |iaceei| %>
  <tr>
    <td><%= iaceei.company_id %></td>
    <td><%= iaceei.value %></td>
    <td><%= link_to 'Show', iaceei %></td>
    <td><%= link_to 'Edit', edit_iaceei_path(iaceei) %></td>
    <td><%= link_to 'Destroy', iaceei, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Iaceei', new_iaceei_path %>
